let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
    
    /* solution */

// It evaluates the right-hand side (count + 1) and then stores that new value back into count
// If count is 0, this becomes 1
// In other words, it takes the old value of count, adds 1, and assigns the result to count
// The assignment operator = puts the new value back into count